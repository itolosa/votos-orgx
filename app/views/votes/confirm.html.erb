<div class="col-sm-6 col-sm-offset-3 text-center ">
  <h2>Confirmación de voto</h2>
  <h5>Usted ha votato por los siguientes candidatos: </h5>

	<div class="table-responsive">
      <table class="table table-hover table-striped">
        <thead>
          <th>Candidatos</th>
        </thead>
        <tbody>
          <% @candidates.each do |candidate| %>
          <tr>
            <td><%= candidate.name %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>

  <p>* Si desea cancelar el voto, vuelva atrás en su navegador</p>
  <div class="actions col-sm-6 col-sm-offset-3 text-center down">
	   <%= form_for(@vote, url: { action: "create" }) do |f| %>
	      <%= f.submit "Confirmar Voto", :class=>'btn btn-lg btn-block btn-success', :onclick => "alert('Voto registrado con éxito!')" %>
	<% end %>
  </div>
</div>

<script type="text/javascript">
 <!--
    function confirm() {
      $('#confirm').submit()
      alert("Su voto ha sido registrado con éxito");
      return false;
    }
 //-->
</script>

