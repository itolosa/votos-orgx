<%= form_for(@front) do |f| %>
  <% if @front.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@front.errors.count, "error") %> prohibited this front from being saved:</h2>

      <ul>
      <% @front.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: 'form-control' %>
  </div>
  <div class="actions">
    <%= f.submit(class: 'btn btn-success') %>
  </div>
<% end %>
