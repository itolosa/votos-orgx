<%= form_for(@vote_candidate) do |f| %>
  <% if @vote_candidate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vote_candidate.errors.count, "error") %> prohibited this vote_candidate from being saved:</h2>

      <ul>
      <% @vote_candidate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :candidate_id %><br>
    <%= f.text_field :candidate_id %>
  </div>
  <div class="field">
    <%= f.label :vote_id %><br>
    <%= f.text_field :vote_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
